# Claims Registry (Thermal-Sponge)

Rule: headline claims must be backed by an evidence ledger:
- `outputs/reports/claim_map.json` (machine-readable)
- `outputs/reports/CLAIM_MAP.md` (human-readable)

## Claims
- C1: stiffness ratio (proxy) vs real
- C2: Pareto claim (requires correct metric mapping + evidence)

Do not publish numbers unless they are mapped in claim_map.json.
